programa {
  funcao inicio() {
    inteiro numero
    inteiro soma = 0
    inteiro contador = 0
    inteiro media

    enquanto(contador < 4){
      escreva("digite seu numero ")
      leia(numero)
      soma = soma + numero
      contador = contador + 1
      
    }
    media= soma / 4

     escreva("A soma dos numeros é: ", soma)
  }
}
